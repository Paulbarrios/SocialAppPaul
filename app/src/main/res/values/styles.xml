<resources>

    <!-- Base application theme. -->
    <style name="MiTema" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="colorPrimary">@color/colorPrimario</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/colorPrimarioOscuro</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="colorAccent">@color/colorAcento</item>
        <item name="android:textColorPrimary">@color/colorTextoPrimario</item>
        <item name="android:colorControlNormal">@color/colorControlNormal</item>
        <item name="android:colorButtonNormal">@color/colorBotonNormal</item>
    </style>

</resources>
